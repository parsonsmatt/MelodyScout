<%= form_for(@release) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= render 'release_dates_form', f: f %>
  <%= render 'contributions_form', f: f %>
 
  <%= f.submit 'Submit', class: 'btn btn-primary' %>
  
<% end %>
