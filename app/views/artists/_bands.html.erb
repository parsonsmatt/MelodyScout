<p>
  <strong>Bands:</strong>
  <ul>
    <% @artist.bands.each do |band| %>
      <li><%= link_to band.name, artist_path(band) %></li>
    <% end %>
    <li><%= link_to 'Add band', '#' %></li>
  </ul>
</p>

