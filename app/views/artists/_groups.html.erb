<h2>Groups:</h2>
<table class="table">
  <tr>
    <th>Name</th>
    <th>Role</th>
    <th colspan='2'>Controls</th>
  </tr>
  <% @artist.band_relations.each do |b| %>
    <tr>
      <td><%= link_to b.band.name, artist_path(b.band) %></td>
      <td><%= b.role %></td>
      <td><%= link_to 'Edit', edit_membership_path(b) %></td>
      <td><%= link_to 'Destroy ', artist_group_path(@artist,b.band), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  <tr>
    <td colspan='3' class='center'>
      <%= link_to 'Add group!', new_artist_group_path(@artist) %>
    </td>
  </tr>
</table>
