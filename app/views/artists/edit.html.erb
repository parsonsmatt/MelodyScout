<% provide :title, "Editing #{@artist.name}" %>
<div class='row'>
    <div class='col-md-8 col-md-push-2'>
        <%= render 'form' %>
        <br>
        <%= link_to 'Show', @artist %> |
        <%= link_to 'Back', artists_path %>
    </div>
    <div class='col-md-2 col-md-pull-8'>

    </div>
    <%= render 'shared/ads' %>
</div>
