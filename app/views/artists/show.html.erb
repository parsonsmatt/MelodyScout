<div class='row'>
  <h1 class='title'><%= @artist.name %></h1>
</div>
<div class='row'>
  <div class='col-md-6 col-md-push-3'>
    
    <%= render 'releases' %>

    <%= render (@artist.group? ? 'members' : 'groups') %>
    
  </div>
  <%= render 'sidebar' %>
  <%= render 'shared/ads' %>
</div>
