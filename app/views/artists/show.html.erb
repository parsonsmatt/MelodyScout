<div class='row'>
  <h1 class='title'><%= @artist.name %></h1>
</div>
<div class='row'>
  <%= render 'sidebar' %>
  <div class='col-md-8 col-md-push-2'>
    
    <%= render 'releases' %>

    <%= render 'members' if @artist.group? %>
    
    <%= render 'groups' if @artist.individual? %>

  </div>
</div>
